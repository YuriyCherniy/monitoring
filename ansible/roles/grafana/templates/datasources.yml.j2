apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ hostvars['monitoring_server']['ansible_private_ip'] }}:9090
    isDefault: true
    editable: false
