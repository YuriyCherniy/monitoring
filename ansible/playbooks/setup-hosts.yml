- name: Prepar all hosts
  hosts: all
  become: true
  tasks:
    - name: Update apt cashe
      ansible.builtin.apt:
        update_cache: yes
        cache_valid_time: 3600
    - name: Upgrade packages
      ansible.builtin.apt:
        upgrade: full
