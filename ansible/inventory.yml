all:
  vars:
    ansible_connection: ssh
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

  children:
    app:
      hosts:
        application:
          ansible_user: ansible
          ansible_host: 111.88.156.96
          os_user: app
    monitoring:
      hosts:
        monitoring:
          ansible_user: ansible
          ansible_host: 111.88.153.210
          os_user: mon
