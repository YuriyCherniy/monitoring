all:
  vars:
    ansible_connection: ssh
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

  children:
    app_servers:
      hosts:
        app_server:
          ansible_user: ansible
          ansible_host: 158.160.26.246
          os_user: app
    monitoring_servers:
      hosts:
        monitoring_server:
          ansible_user: ansible
          ansible_host: 158.160.14.79
          os_user: mon
