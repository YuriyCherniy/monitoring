all:
  vars:
    ansible_connection: ssh
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

  children:
    app_servers:
      hosts:
        app_server:
          ansible_user: ansible
          ansible_host: 130.193.41.231
          ansible_private_ip: 10.129.0.26
          os_user: app
    monitoring_servers:
      hosts:
        monitoring_server:
          ansible_user: ansible
          ansible_host: 158.160.81.139
          ansible_private_ip: 10.129.0.22
          os_user: mon
